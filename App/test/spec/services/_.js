'use strict';

describe('Service: _', function () {

    // load the service's module
    beforeEach(module('limonadaApp'));

    // instantiate service
    var _;
    beforeEach(inject(function (___) {
        _ = ___
    }));

    it('should _ exists', function () {
        expect( !! _).toBe(true);
    });
});